# ICP 备案成功后检查清单

备案最终由接入商和通信管理局审核。本清单只用于正式通过后的人工恢复与复查。

## 恢复前

1. 获取真实 ICP 备案号。
2. 确认备案主体、网站名称“我们的小宇宙”、域名 `xiaoxingxing.love` 信息一致。
3. 填入 `ICP_NUMBER`。
4. 保持 `ICP_URL=https://beian.miit.gov.cn/`。
5. 确认没有填写申请中、猜测或他人的备案号。
6. 确认 `PSB_NUMBER` 和 `PSB_URL` 仍为空，除非公安联网备案已经真实完成。
7. 将数据导出、服务端脚本副本、构建日志、审计报告等不应公网访问的文件移出前端 web 根目录，或配置部署层禁止访问。
8. 确认 `BUILD_LOG.md` 没有被提交或上传。

## 恢复网站

1. 前端恢复正式 `vercel.json` 路由，移除审核期 `/(.*)` 到 `icp-review.html` 的 catch-all。
2. 提交并推送前端变更。
3. 等待 Vercel Production 状态 Ready。
4. 后端生产 `.env` 设置：

```ini
ICP_REVIEW_MODE=false
ICP_NUMBER=正式下发的备案号
ICP_URL=https://beian.miit.gov.cn/
PSB_NUMBER=
PSB_URL=
```

5. 重启 API：

```bash
pm2 restart love-timer-api --update-env
```

## 恢复后必须复查

1. 首页、相册、清单、点滴、信件、关于我们正常展示站主个人内容。
2. 普通访客不能留言投稿。
3. 普通访客不能上传图片。
4. 普通访客不能添加、修改、删除任何内容。
5. 管理后台需要服务端身份验证。
6. 后台写接口需要服务端验证，不依赖 localStorage。
7. 管理入口不要长期暴露在 PUBLIC 页面显眼位置。
8. 文件上传限制只允许必要图片类型，限制大小，禁止脚本/HTML/SVG 风险文件。
9. 第三方脚本只恢复真正需要的 CDN，不添加统计、广告或评论服务。
10. 音乐版权不明确时，不要向公网提供完整音乐文件。
11. 检查照片 EXIF 和页面文字，避免公开身份证、手机号、住址、精确定位、证件照、验证码、后台密码或密钥。
12. 检查 `.env`、数据库、备份文件、`.git` 不可公网访问。
13. 检查目录浏览关闭。
14. 检查 HTTP、HTTPS、www、裸域都进入同一个正式站。
15. 检查备案号在页脚展示，点击跳转工信部备案系统。
16. 开始公安联网备案。
17. 公安备案成功后再填写真实 `PSB_NUMBER` 和 `PSB_URL`。
18. 不伪造任何 ICP 或公安备案编号。

## 不建议恢复为公众开放的功能

- 匿名游客留言
- 游客上传图片
- 游客发布点滴/文章
- 公众注册
- 评论区
- 论坛
- 社区
- 公众交友
- 会员、充值、付费、商城、广告招商等商业内容
- API 调试、测试、数据库管理页面

